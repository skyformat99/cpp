include_directories(include)
add_executable (spdlog_demo spdlog_demo.cc)
find_library(HAVE_PTHREAD pthread)
if (HAVE_PTHREAD)
	message("Pthread found")
else (HAVE_PTHREAD)
	message("Pthread not found")
endif(HAVE_PTHREAD)
target_link_libraries(spdlog_demo pthread)
